web: openfisca serve --port $PORT --workers "$WORKERS" --tracker-idsite "$TRACKER_ID" --tracker-url "TRACKER_URL" --country-package openfisca_france --tracker-token "$TRACKER_TOKEN" --welcome-message "$WELCOME"

